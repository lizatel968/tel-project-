#include <iostream>
#include <string>
using namespace std;

int hamming(string x, string y){
  int set;
  set = 0;
  for(int i=0;i<a.length();++i){
    if(a[i] != b[i]){
      set++;
    }
  }
  return set;
}
int main()
{
  string x, y;
  cin>>x>>y;
  
  cout<<hamming(x, y);
  
  return 0;
}
